{
  "name": "github-release-notifier-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:e2e": "playwright test --config=playwright.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.3"
  },
  "dependencies": {
    "github-release-notifier": "file:.."
  }
}
